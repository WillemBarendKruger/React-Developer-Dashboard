import { UserCard } from "../components/UserCard"

export const Home = () => {
    return(
        <div>
            {/* Header with searchbar & navigation controls */}

            {/* User cards */}
            <UserCard />
        </div>
    )
}